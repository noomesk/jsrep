//datos primitivos: string, number, boolean, null, undefined, symbol
let numero = 10; //number
let texto = "Hola amikos"; //string
let verdadero = true; //boolean
let falso = false; //boolean
let noDefinido; //undefined
let undef = undefined; //undefined
let nulo = null; //null
//null se asigna cuando queremos indicar que una variable no tiene valor, mientras que undefined se asigna automáticamente a una variable que ha sido declarada pero no inicializada. 